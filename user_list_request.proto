syntax = "proto3";

package flare;

// Message type: client to server

// Denotes the request to view a list of all users registered on the server
// See: UserListResponse 

message UserListRequest { };