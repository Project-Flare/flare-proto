syntax = "proto3";

package flare;

// Message type: client to server

// Denotes the initiating message for a login exchange, where the client requests an authorization token to use semi-permanently
// See: AuthRequest, AuthResponse

message LoginRequest {
    string username = 1;
    string password = 2;
}