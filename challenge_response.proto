syntax = "proto3";

package flare;

// Message type: client to server

// Denotes the proof of work response where the preimage of the challenge is submitted for verification
// See: ChallengeIssuance

message ChallengeResponse {
    string preimage = 1;
}